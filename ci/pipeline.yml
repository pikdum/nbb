# vim:ft=ansible:
resources:
  - name: nbb
    type: git
    source:
      uri: http://git.kuudere.moe/pikdum/nbb
      branch: master
  - name: package
    type: git

jobs:
  - name: build
    plan:
      - get: nbb
        trigger: true
      - task: build
        file: nbb/ci/build.yml
  - name: deploy
    plan:
      - task: deploy
        config:
          platform: linux
          inputs:
            - name: package
          image_resource:
            type: docker-image
            source: {repository: node}
          run:
            path: echo
            args: ["{{ test}}"]
